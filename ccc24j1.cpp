#include <bits//stdc++.h>
/*
Each red plate of sushi costs $3, each green plate of sushi costs $4, and each blue plate of
sushi costs $5.
*/
using namespace std;

int main () {
    int r;
    int g;
    int b;
    cin >> r;
    cin >> g;
    cin >> b;
    cout << (r * 3) + (g * 4) + (b * 5); 
}